<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>News</title>
    </head>

    <body>
        <h1>News</h1>

        <table>
            <tr>
                <td>ID</td>
                <td>Description</td>
            </tr>
            
            <% for(var i=0; i < news.length; i++) { %>
                <tr>
                    <td><%= news[i].id %></td>
                    <td><%= news[i].titulo %></td>
                </tr>
            <% } %>
        </table>
    </body>
</html>